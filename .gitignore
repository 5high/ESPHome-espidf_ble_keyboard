CLAUDE.md
To_do.txt

# ESPHome build output
.esphome/

# Python bytecode
__pycache__/
*.pyc

# Local verification configs
_verify_*.yaml

# Editor tasks for building locally — machine-specific paths, and building is
# deliberately not a GitHub concern.
.vscode/

# Claude Code settings and hooks — machine-specific absolute paths, and a
# maintainer-only reminder has no business running for anyone who clones this.
.claude/

# Local-only workbench: downloaded reference material, the converted remote-style
# catalogue and the pages that display it. Never published — the styles in here
# are data to paste into Remote Style > Import, not part of the firmware.
local/